##User-Define Types (UDTs)

User-defined types (UDTs) allow custom data types to be defined. A UDT can combine commonly coupled data within a single type, simplifying retrieval of the information in queries.

[Top &#8593;](#sections)
